# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run

# Forge's log4j config writes here from the repo root, so every ./gradlew build leaves a logs/
# behind in the working tree - test output, not anything the mod ships.
logs

# Files from Forge MDK
forge*changelog.txt
